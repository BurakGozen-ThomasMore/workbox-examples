self.addEventListener("message", async (event) => {
  if (event.data.command === "ACTIVATE_OFFLINE_NOTIFICATIONS") {
    /**
     * Vul deze functie verder aan zodat we:
     * - om de 10 seconden een melding krijgen van de app
     * - Dit aan en uit kunnen zetten
     */
  }
});
